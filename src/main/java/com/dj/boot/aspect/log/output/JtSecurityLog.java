/*
package com.dj.boot.aspect.log.output;

import com.alibaba.fastjson.JSON;
import com.dj.boot.aspect.log.log4j.JtSecurityLoggerFactory;
import org.apache.logging.log4j.Logger;

*/
/**
 * 安全部规定日志
 * 包括 head(头部)，reqInfo(请求部分)，respInfo(响应部分)
 *//*

public class JtSecurityLog extends SecurityLog {

    //安全日志Logger
    private static final Logger JT_LOGGER = JtSecurityLoggerFactory.getLogger();


    public JtSecurityLog(HeadLogSecurityInfo head, ReqLogSecurityInfo reqInfo, RespLogSecurityInfo respInfo) {
        super(head, reqInfo, respInfo);
    }

    public void print() {
        JT_LOGGER.info(JSON.toJSONString(this));
    }
}

*/
